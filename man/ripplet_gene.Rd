% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ripplet_gene.R
\name{ripplet_gene}
\alias{ripplet_gene}
\title{Run the Full RIPPLET Pipeline}
\usage{
ripplet_gene(
  edgelist,
  mutmat,
  alpha = 0.7,
  maxiters = 20000,
  eps = 1e-14,
  k = 20,
  delta = 0.2,
  verbose = TRUE
)
}
\arguments{
\item{edgelist}{A two‑column \code{data.frame} or matrix of gene–gene edges.}

\item{mutmat}{A numeric matrix (genes × samples) of 0/1 mutations (or weights).}

\item{alpha}{Restart probability for RWR (default 0.7).}

\item{maxiters}{Maximum iterations for RWR (default 20000).}

\item{eps}{Convergence tolerance for RWR (default 1e-14).}

\item{k}{Number of components for sample‐similarity SVD (default 20).}

\item{delta}{Decay parameter for weighted scores (default 0.2).}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages (default \code{TRUE}).}
}
\value{
A numeric matrix (genes × samples) of weighted cosine scores.
}
\description{
From an edge list and mutation matrix, this function:
\enumerate{
\item Builds and simplifies the network adjacency matrix
\item Normalizes it to row‑stochastic form
\item Runs Random Walk with Restart for each sample
\item Computes sample–sample similarity via truncated SVD
\item Computes cohort‑informed, weighted cosine gene scores
}
}
\examples{
\dontrun{
# 1) build & normalize adjacency
adj      <- build_network_adj(edgelist)
adj_norm <- normalize_network_adj(adj)

# 2) run full pipeline
result <- ripplet_gene(
  edgelist, mutmat,
  alpha    = 0.85,
  maxiters = 5000,
  eps      = 1e-8,
  k        = 10,
  delta    = 0.1,
  verbose  = FALSE
)
}
}
