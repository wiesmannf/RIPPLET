% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ripplet_pathways.R
\name{get_ripplet_pathways}
\alias{get_ripplet_pathways}
\title{RIPPLET Pathway Pipeline}
\usage{
get_ripplet_pathways(
  data.base = "reactome",
  dampening_alpha = 0.99,
  show_progress = TRUE
)
}
\arguments{
\item{data.base}{Character scalar, pathway database (e.g., "reactome", "kegg"). See \code{graphite::pathwayDatabases()} to see all databases.
Passed to \code{\link{get_graphs}}.}

\item{dampening_alpha}{Numeric in (0,1]; dampening factor for near-singular
matrices in \code{\link{norm_graphs}} (default 0.99).}

\item{show_progress}{Logical; if \code{TRUE}, show a progress bar for MaxHit
(requires the \pkg{progress} package). Default \code{TRUE}.}
}
\value{
A list in the same format as \code{\link{norm_graphs}} with
an additional final element:
\itemize{
\item \code{INFO}
\item \code{Adj_Matrices}
\item \code{SPIA_Norm_Adj_Matrices}
\item \code{MaxHit} (named numeric vector of maximum propagation scores)
}
}
\description{
Convenience wrapper that:
(1) fetches pathways via \pkg{graphite},
(2) creates (normalized) adjacency matrices for topological pathway scoring, and
(3) computes per-pathway Maximum Propagation Scores (MaxHit) for downstream pathway perturbation score normalization (PPS to NPPS)
}
\examples{
\dontrun{
out <- get_ripplet_pathways(data.base = "reactome",
                          dampening_alpha = 0.99,
                          show_progress = FALSE)
head(out$INFO)
head(out$MaxHit)
}

}
